################# java.lang.Integer  ################# 

static int MAX_VALUE -- 这是一个常数持有的最大的一个int值, 231-1.
static int MIN_VALUE -- 这是一个常数持有的最小值int为, -231.
static int SIZE -- 这是数位用来表示一个int值的二进制补码形式.
static Class<Integer> TYPE -- 这是类的实例，表示基本类型int

Integer(int value)
Integer(String s)

byte byteValue()
double doubleValue()
float floatValue()
int intValue()
long longValue()
short shortValue()

static String toBinaryString(int i)
static String toHexString(int i)
static String toOctalString(int i)
static String toString(int i)

################# Java.lang.Enum.Float  ################# 
static int MAX_EXPONENT -- 这是最大的指数可能有一个有限的浮点型变量.
static float MAX_VALUE -- 这是一个常量浮点类型的值最大的正有限, (2-2-23)·2127.
static int MIN_EXPONENT -- 这是最低的指数归一化的浮点型变量可能有.
static float MIN_NORMAL -- 这是一个常量浮点类型的值最小正值, 2-126.
static float MIN_VALUE -- 这是一个常量浮点型（float）的最小正非零值, 2-149.
static float NaN -- 这是一个不断举行一个非数字（NaN）值浮点型（float）.
static float NEGATIVE_INFINITY -- 这是一个常量负无穷大，浮点型（float）.
static float POSITIVE_INFINITY -- 这是一个常量浮点型（float）正无穷大.
static int SIZE -- 这是一个浮点值，用于表示的比特数.
static Class<Float> TYPE -- 这是类的实例，代表float基本类型.

Float(double value) 
Float(float value) 
Float(String s) 

byte byteValue()
double doubleValue()
float floatValue()
int intValue()
long longValue()
short shortValue()
 
static String toHexString(int i)
static String toString(int i)



################# java.lang.Byte  ################# 
static byte MAX_VALUE -- 保持的最大的值，可以有一个字节，这是恒定，最大值为： 27-1.
static byte MIN_VALUE -- 保持可以有一个字节的最低值，这是一个恒定的，最大值为：-27.
static int SIZE -- 这是用来表示一个字节的值在2补码二进制形式的比特的数目。
static Class<Byte> TYPE -- 这是类的实例，原始类型字节。

   
Byte(byte value) 
Byte(String s) 

byte byteValue()
double doubleValue()
float floatValue()
int intValue()
long longValue()
short shortValue()

################# java.lang.Character  ################# 

Character(char value) 
构造一个新分配的字符表示指定char值的对象。


################# java.lang.StringBuilder  ################# 
java.lang.StringBuilder 类是可变的字符序列。这提供了一个与StringBuffer兼容的API，但不保证同步

1 	StringBuilder() 
构造一个字符串生成器中没有字符，其初始容量为16个字符.
2 	StringBuilder(CharSequence seq) 
构造一个字符串生成器包含相同的字符指定的CharSequence.
3 	StringBuilder(int capacity) 
构造一个字符串生成器中没有字符，其初始容量的容量参数指定.
4 	StringBuilder(String str) 
构造一个字符串生成器初始化为指定字符串的内容.

char charAt(int index)
void setCharAt(int index, char ch) 
StringBuilder deleteCharAt(int index)

StringBuilder append(String str) 
StringBuilder insert(int offset, String str) 