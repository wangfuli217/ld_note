scullp: 使用整页scull
scullc: 使用slab的scull
scullv: 使用高端内存的scull
misc-progs/inp.c  用户空间访问IO端口
misc-progs/outp.c 用户空间访问IO端口
short      控制并口：简单IO控制
    /dev/short0  inb outb 
    /dev/short0p inb_p outb_p
    /dev/short0s insb outsb
shortprint 控制并口：实现了打印协议握手
simple mmap函数实现  实现了remap_pfn_range和nopage两种方式。

/drivers/char/mem.c 内存映射例子


LDD3-6/scullp: ioctl
LDD3-62/pipe: block nonblock sync
LDD3-63/scullpipe: poll
LDD3-64/scullpipe: SIGIO
LDD3-7/jit.ko                           
        "jitimer"       超时定时器
        "jitasklet"     tasklet
        "jitasklethi"   high tasklet
1. 用于内核任务同步用户态进程，或者内核任务同步内核进程，内核进程同步用户态进程，用户态进程同步用户态进程。
2. 不能用于内核进程、用户态进程同步内核任务或内核任务同步内核任务。
init_waitqueue_head
wait_event_interruptible(data->wait, !data->loops);
wake_up_interruptible(&data->wait);

"currentime" 获取当前时间        时间函数说明
"jitbusy"    CPU忙等待           cpu_relax 函数
"jitsched"   进程忙调度等待      schedule函数
"jitqueue"   进程超时或触发等待  wait_event_interruptible_timeout函数
"jitschedto" 进程超时等待        schedule_timeout函数
