面试题：谈谈嵌入式linux系统软件的认识？
1.TPAD烧写bootloader(u-boot)
  bootloader:统称
  u-boot属于bootloader的一种
  S5PV210处理器启动方式：
  Nand
  USB
  SD
  UART
  JTAG
  "启动方式"：上电以后,CPU硬件自动去读取相应的
              外设,例如如果是从Nand启动,CPU硬件
              自动读取Nand的0地址,如果读到程序以后
              将程序装载到内存中运行,这个程序肯定
              是bootloader(u-boot)
  启动方式的选择通过TPAD的拨码开关
  
  烧写uboot可以采用USB下载或者SD卡下载
  
2.采用USB烧写
  必须采用DNW软件
  设置启动方式为USB
  COM:串口
  	公头
  	母头
  	115200,COM1(台式机)
  	笔记本：买一个USB转串口(30-100元)
  USB:USB口
