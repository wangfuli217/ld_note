Back in the dark mists of time it was recommended practice for C programmers to generate 
C code from their Glade UI designs. Glade version 2 supports this directly with the Build button on the toolbar, 
however you should ignore that feature as it only applies to C. 
Indeed the recommendation these days鈥攅ven for C programmers鈥攊s to load the UI at runtime, as this is much more maintainable and flexible.

every widget has its own type class & type

event driven. The mainGUI function will sleep until something happens
