--[=[
unpack (list [, i [, j]])
    返回指定表的索引的值,i为起始索引，j为结束索引

    注：本函数只能用于以数字索引访问的表,否则只会返回nil如：t={"1","cash"}
E: too many results to unpack
--]=]

-- i=0, j=#list
print(unpack{10,20,30}) -- unpack({10,20,30})
a,b = unpack{10,20,30}  -- unpack({10,20,30})
print(a,b)

-- i=2, j=#list
print(unpack({10,20,30},2))

-- i=1, j=2
print(unpack({10,20,30},1,2))

