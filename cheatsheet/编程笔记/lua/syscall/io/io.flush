--[[
    file:flush()
    将写入的数据保存到 file 中。
    
    io.flush() 运行io.flush()会保存所有要写入输出默认文件的数据.
    等价于 io.output():flush()。
    
    flush函数将所有还未写入文件的信息执行写入操作。
    可以以函数形式使用：io.flush(),将会flush当前的输出文件；
    也可以以方法形式使用：f:flush(),将特定的文件执行flush操作。
--]]
