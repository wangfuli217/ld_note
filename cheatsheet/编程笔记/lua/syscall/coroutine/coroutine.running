--[[
  coroutine.running()
  返回当前的协程,如果实在主线程,则返回nil
--]]

local co = coroutine.create(
    function () 
        print(coroutine.running()) 
        end)

print(coroutine.running())
coroutine.resume(co)
print(co)
