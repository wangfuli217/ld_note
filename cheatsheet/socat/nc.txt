nc [-46DdhklnrStUuvzC] [-i interval] [-p source_port] [-s source_ip_address] [-T ToS] [-w timeout]
   [-X proxy_protocol] [-x proxy_address[:port]] [hostname] [port[s]]
   
unix                          -> -u udp -Z dccp other tcp -n 名字反向解析
    nc -lU /var/tmp/dsocket
    nc -U -s /var/tmp/dsocket
listen
    nc -l [hostname] port
    nc -l -p port # 不建议
其他
    nc hostname port
    
-x addr[:port]