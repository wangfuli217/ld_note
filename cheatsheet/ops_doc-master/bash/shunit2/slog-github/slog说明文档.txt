cat - << 'EOF'
[概要设计]
    [slog_p_idea]
[设计样板]
    tests/sloggi.sh
EOF

slog_p_idea(){ cat - << 'EOF'
POSIX compatible logging library for bash, dash, zsh, and others

[导入]
source  slog.sh

[调用函数]
log_info
log_warning
log_success
log_error
log_debug
Prints a "DEBUG" level message to stdout and/or file with the timestamp of the occurrence. Disabled, by default.

[环境变量]
LOG_PATH :        将输出目的地由stdout,转向该环境变量指定的文件
LOG_LEVEL_STDOUT: 修改输出stdout日志的级别
LOG_LEVEL_LOG   : 修改输出文件日志的级别
EOF
}