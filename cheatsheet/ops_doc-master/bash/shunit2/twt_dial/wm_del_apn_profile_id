# !/bin/sh
# default
# ubus call sdk_service.wm del_apn_profile_id '{ "profile_id":"7" }'
PROFILE_ID=${1:-7}
ubus call sdk_service.wm del_apn_profile_id $(
  cat <<EOF
{"profile_id":"${PROFILE_ID}"}
EOF
)
