
1)create a new repo on github, with README.md;
2)git clone to local: git clone https://github.com/lianzeng/newRepo.git
3)git add/git commit/ git push; 
4)the third step  will request github username/pwd when push, suggest to add the local author ssh key to github to avoid username/pwd each time.
