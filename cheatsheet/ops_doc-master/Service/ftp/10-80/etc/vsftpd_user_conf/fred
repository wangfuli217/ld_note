anon_world_readable_only=NO
write_enable=YES
anon_upload_enable=YES
# must user virtual home directory
# sudo chmod u-w ftpsite
# sudo chmod -R u=rw ftpsite/*
# sudo chmod -R go=r ftpsite/*

local_root=/home/ftpsite
# dirlist_enable=NO
# hide_ids=YES
