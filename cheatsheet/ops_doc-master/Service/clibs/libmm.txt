本文的 libmm 除了 Share  Memory ，也可做 Memory  Pool ⽤，就是⽤ mmap 预先划出⼀⼤块内
存，以后的分配操作都可以在这块内存内部进⾏，包括 malloc、calloc 、free  等等。
Memory  Pool 的好处是不在堆和栈上分配，可以重复使用，避免多次向内核请求分配和释放内存，
⼀定程度上提⾼了性能。另外只需释放整个 Pool 即可完成所有的内存释放，避免内存泄露的发⽣。

sudo apt-get libmm14 libmm-dev libmm-dbg
