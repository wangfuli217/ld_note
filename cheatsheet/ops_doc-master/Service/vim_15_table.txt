https://www.jianshu.com/p/6aa2e0e39f99
https://www.cnblogs.com/imzye/p/8496120.html

https://kenvifire.gitbooks.io/vimscript/content/index.html

buffer 缓冲区 --> :b :buffers
register 寄存器 --> "（存入或取出）
marcro 宏 qa(记录在寄存器a中) --> @a(执行寄存器里面的命令)
mark 标记 ma --> 'a 或 g'a
g#(匹配的是word) #(匹配的是\<word\>单个词) g* *
vim中输入:h! :h 42

find . -maxdepth 1 -name xx.txt | xargs vim +"argdo se bomb | se fileencoding=utf-8 | wq" 
find . -maxdepth 1 -name xx.txt | xargs vim +"argdo set bomb | set fileencoding=utf-8 | wq"

1. 进入vim
vim filename            打开或新建文件，并将光标置于第一行首
vim +n filename         打开文件，并将光标置于第一n行
vim +  filename         打开文件，并将光标置于最后一行
vim +/pattern filename  打开文件，并将光标置于第一个与pattern匹配的串处
vim -r filename         在上次正用vim编辑时发生系统崩溃，恢复filename
vim filename..filename  打开多个文件依次编辑



2. vim配置
all             列出所有选项设置情况
term            设置终端类型
ignorance       在搜索中忽略大小写
list            显示制表位(Ctrl+I)和行尾标志（$)
number          显示行号
report          显示由面向行的命令修改过的数目
terse           显示简短的警告信息
warn            在转到别的文件时若没保存当前文件则显示NO write信息
nomagic         允许在搜索模式中，使用前面不带"\"的特殊字符
nowrapscan      禁止vi在搜索到达文件两端时，又从另一端开始
mesg            允许vi显示其他用户用write写到自己终端上的信息
:set ruler /set noruler     显示/不显示标尺
:set hlsearch   高亮显示查找到的单词
:set nohlsearch 关闭高亮显示
:syntax on      语法高亮
:set nu         显示行号
:set autoindent 自动缩进
:set cindent    C语言格式里面的自动缩进

3. 移动光标
)       光标移至句尾(句号分隔)
(       光标移至句首(句号分隔)
}       光标移至段落开头{空行分隔}
{       光标移至段落结尾{空行分隔}
n$      光标移至第n行尾
15|     移动到当前行的 15列
H       光标移至屏幕顶行
M       光标移至屏幕中间行
L       光标移至屏幕最后行
%       移动到与制匹配的括号上去（），{}，[]，<>等
nG      移动到第n行上
;       跳到下一个 f/t 搜索的结果
,       跳到上一个 f/t 搜索的结果

4. 屏幕滚动
nz      将第n行滚至屏幕顶部，不指定n时将当前行滚至屏幕顶部

5. 插入文本类
R       替换当前字符及其后的字符，直至按ESC键
s       替换字符（删除光标处字符，并进入插入模式，前可接数量） 
S       替换行（删除当前行，并进入插入模式，前可接数量）
gi      进入到上一次插入模式的位置


6. 删除命令
C$          改变到行尾
cc          改变整行
shift+j     删除行尾的换行符，下一行接上来了

7. 复制粘贴

8. 撤销
u           撤销前一次的操作
shif+u(U)   撤销对该行的所有操作

9. 搜索及替换
:s/p1/p2/g                  将当前行中所有p1均用p2替代,g表示执行 用c表示需要确认
:n1,n2 s/p1/p2/g            将第n1至n2行中所有p1均用p2替代
:g/p1/s//p2/g               将文件中所有p1均用p2替换
:1,$ s/string1/string2/g    在全文中将string1替换为string2

10.书签
m[a-z]  在文中做标记，标记号可为a-z的26个字母
`a      移动到标记a处

11. visual模式

12. 行方式命令
:n1,n2 co n3        将n1行到n2行之间的内容拷贝到第n3行下
:n1,n2 m n3         将n1行到n2行之间的内容移至到第n3行下
:n1,n2 d            将n1行到n2行之间的内容删除
:n1,n2 w!command    将文件中n1行至n2行的内容作为command的输入并执行之
若不指定n1，n2，则表示将整个文件内容作为command的输入

13. 宏


14. 窗口操作
:split          分割一个窗口
:split file.c   为另一个文件file.c分隔窗口
:nsplit file.c  为另一个文件file.c分隔窗口，并指定其行数
ctrl＋w         在窗口中切换
:close          关闭当前窗口

15. 文件及其他
:e!             放弃修改文件内容，重新载入该文件编辑
:r!command      将命令command的输出结果放到当前行
:read file.c    将文件file.c的内容插入到当前光标所在的下面