1. cmdline
start stop restart monitor reload status summary report quit validate procmatch test # monit
start stop restart reload info test kill                                             # mfsmaster
start stop restart reload abort flush check status condrestart                       # /etc/init.d/postfix 


2. configure
2.1 代码内部设定配置默认值；
2.2 命令行和配置同时存在是，命令行优先
2.3 表示名和变量类型关联起来： (moosefs)
2.4 支持动态、扩展性配置 (monit)

3. env