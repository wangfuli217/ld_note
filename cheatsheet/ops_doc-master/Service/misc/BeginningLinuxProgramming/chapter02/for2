#!/bin/sh

#---------------------------------------------------
#	`ls f*` 和$(ls f*)是等价的
#	$该符号也说明了，他其实就是运行shell
#	只是简单的取值更为简单点
#---------------------------------------------------

for file in $(ls f*); do
    echo $file
done

exit 0

