
int Net_setNonBlocking(int socket);
int Net_setBlocking(int socket);

int Net_canRead(int socket, time_t milliseconds);   # poll能读
int Net_canWrite(int socket, time_t milliseconds);  # poll能写

ssize_t Net_read(int socket, void *buffer, size_t size, time_t timeout);        # 部分或全部读，屏蔽了EWOULDBLOCK
ssize_t Net_write(int socket, const void *buffer, size_t size, time_t timeout); # 部分或全部写，屏蔽了EWOULDBLOCK

int Net_abort(int socket);

int Net_shutdown(int socket, int how);
int Net_close(int socket);