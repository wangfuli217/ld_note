#!/bin/sh

source commands.sh

helu foo
from tqbf@pobox.com

X=1
while [ $X -lt 1000 ]; do
    to `h8 5`@sockpuppet.org
    X=`expr $X + 1`
done   

msg
header Subject lots of recipients
body This is a test
eom

