==============
keywords
==============

1. 	testUpperVPath.mk

	1) VPATH
		prerequisites	$^
	
	2) GPATH
		./src is ok
		src is not ok
		target 			$@


2.	testUpperVPathWithInc.mk
	1) MAKEFILE's  VPATH  is not equil  cc path
	2) linux 's PATH is not equil cc's path
	3) -I./inc

3.	testlowerVPath.mk
	1) vpath's order
	2) vpath %.x [path1] [path2] [path3]
	3) vpath %.x [path1]
	   vpath %.x [path2]
	   vpath %.x [path3]
	4) reset vpath for %.x
	   vpath %.x

