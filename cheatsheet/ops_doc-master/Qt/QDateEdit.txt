
widgets\widgets\spinboxes\Window.cpp  
   QDateEdit *dateEdit = new QDateEdit(QDate::currentDate());
   setDateRange(QDate(2005, 1, 1), QDate(2010, 12, 31));
   setText(tr("Appointment date (between %0 and %1):")
      .arg(dateEdit->minimumDate().toString(Qt::ISODate))
      .arg(dateEdit->maximumDate().toString(Qt::ISODate)));
      
QTimeEdit *timeEdit = new QTimeEdit(QTime::currentTime());
   setTimeRange(QTime(9, 0, 0, 0), QTime(16, 30, 0, 0));
   setText(tr("Appointment time (between %0 and %1):")
      .arg(timeEdit->minimumTime().toString(Qt::ISODate))
      .arg(timeEdit->maximumTime().toString(Qt::ISODate)));
      
meetingEdit = new QDateTimeEdit(QDateTime::currentDateTime());


用于接收时间的输入，包括时、分、秒。它的时间属性类型是 QTime，访问函数为：
QTime time() const
void setTime(const QTime & time)
当时间属性被修改时，会触发信号：
void timeChanged(const QTime & time) 



